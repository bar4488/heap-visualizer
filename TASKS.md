# Improvement tasks (2026-07-06)

Batch of UX improvements requested for the viewer. Checked off as implemented.

- [x] **1. Step selects the related allocation** — stepping fwd/back selects (outlines)
  the allocation the malloc/free/realloc touches, not just centers it.
- [x] **2. Event list panel** — "Events" toolbar button opens a virtualized, sequenced
  list of all events; clicking one seeks there and selects the allocation it
  touches; a "follow" checkbox keeps the current event in view.
- [x] **3. Horizontal zoom on the address line** — timeline-style zoom of the byte
  (x) axis; row size is untouched. Ctrl/alt+wheel zooms around the cursor,
  shift+wheel / horizontal scroll pans, the toolbar shows `↔ ×N` with click-to-
  reset. Picking, hover, overlays, marks and shift+click honor the zoom.
- [x] **4. Select-all / deselect-all on checkbox lists** — "all · none" links on the
  filter panel groups (sites, threads) and the tags list.
- [x] **5. Locked viewport mode** — 🔓/🔒 toggle next to the step buttons (key: L);
  when locked, stepping keeps the address view anchored (no auto-scroll).
- [x] **6. Tag by frees** — the range-selection popover has "Tag allocs" (created in
  range) and "Tag freed" (freed in range) actions.
- [x] **7. Highlight mallocs when stepping** — stepping onto an M event outlines the
  new allocation in green (like frees flash red) — small mallocs are findable.
- [x] **8. Marked addresses always reachable** — address marks are pinned: their rows
  stay in the layout even when nothing is live there.
- [x] **9. Don't get scrolled away when everything is freed** — the scroll anchor
  (top-of-viewport address) is transiently pinned across seeks, so the row you
  are looking at survives even if all its allocations are freed.
- [x] **10. "all rows" mode** — toolbar checkbox that lays out every row any
  allocation *ever* touches (playhead-independent), so the map never reflows
  as allocations come and go.
- [x] **11. In-allocation labels** — centered label inside each allocation wide
  enough: "name · size" if it fits, else the name, else the size. Toggleable
  via the "sizes" checkbox; the size format is controlled from Layout, and
  names come from the Analysis panel.
- [x] **12. Flash-where-is-it from the event list** — clicking the already-current
  event in the Events panel scrolls/pans to it and pulses its exact location
  (rect flash + expanding ping ring for tiny allocations).
- [x] **13. Fix: overlay highlights offset to the right** — the addr canvas was
  sized from the scroll container (which includes the scrollbar) but displayed
  at content width; now measured from `#addr-view` so raster and overlays align.
- [x] **14. Stepping opens the allocation dialog** — stepping onto (or jumping to)
  a malloc/free/realloc fills and opens the detail panel for the allocation it
  touches (`hp_alloc_info`, shared with the pick path).
- [x] **15. Layout dialog** — the address-line layout controls (row bytes,
  collapse, all rows, sizes, row zoom, color) moved from the toolbar into a
  "Layout" panel behind a toolbar button.
- [x] **16. Draggable, stacking panels** — every panel is a window: drag it by
  its header; the last panel opened or touched rises to the top of the stack.
- [x] **17. Demo seq warning fixed** — gen.py gave the header `seq:0` and started
  events at 1; per spec §3.7 seq is the 0-based index among event records only.
  The header no longer consumes a seq and `web/demo.heapl` was regenerated.
- [x] **18. Label on the middle line** — a multi-row allocation's name/size label
  sits on the middle of its visible rows (rounded to the top), not the first.

## Verification
- [x] `cargo test` in `core/` — 17 tests pass (new: tag-by-free, pinned rows,
  anchor pin, show-all layout, x-zoom picking, malloc move-link)
- [x] `./build.sh` — wasm builds and staged into `web/`

# Spec v2 implementation (2026-07-13)

Per docs/spec-v2-draft.md + followup answers (docs/spec-v2-followup2.md).

- [x] **Format v2 (core)** — `seq`/`id` removed from the wire; frees key by
  address; invalid-free & overlap are anomalies (overlap implicitly ends the
  victim, replayed correctly both seek directions); header `v:2`.
- [x] **Spans & logs (core)** — `B`/`E` matched per lane (thread lanes +
  global lane, orphan-E and open-at-EOS rules per spec I.3), `L` records;
  exposed via `hp_spans_json` / `hp_logs_json` / events JSON.
- [x] **Run merge (core)** — multi-file parse staged per file, k-way merged
  by `t` (stable, earlier file wins ties); `hp_parse_begin(n)`/`hp_file_begin`.
- [x] **Web v2** — events panel + step readout for B/E/L, anomaly rows in
  warnings, birth-index identity (no more ids), v2 demo (gen.py emits
  spans/logs; threads=4).
- [x] **Project model** — landing screen with recents (IndexedDB),
  project.json manifest (generated by scan, hand-editable), runs, paired
  auto-saved .heapa (includes window layout), ephemeral quick-look +
  save-as-project; Project window lists runs/files (dockable).
- [x] **Bridge** — bridge/heapviz-bridge.py (stdlib, 127.0.0.1, token,
  traversal-safe, CORS) + BridgeBackend in the web app.
- [x] **Lanes (first slice)** — span flame ribbons (global + per-thread) and
  log ticks on the temporal axis; wheel zoom/pan + dblclick like the strips
  (shared view); spans/logs selectable → Span/Log window (go to begin/end,
  zoom, crop); Lanes panel picks visible lanes (persisted; default global +
  logs to stay calm); stepping onto B/E/L opens the Span/Log window.
- [x] SPECIFICATION.md rewritten as v2 (draft merged; deferred: invalid-free
  ghost flash, tool rename).

Remaining (next batch):
- [ ] Analysis spans (user-created, incl. on thread lanes — followup2 #2),
  shift-drag on lanes, dashed rendering, stored in .heapa.
- [ ] Full lane model: sequential-axis lanes, add/remove/reorder beyond
  visibility toggles, span-based filtering ("during frame") and
  color-by-caller attribution.
- [ ] Log importer (timestamp pattern → L-only sidecar) + per-file import
  settings in project.json.
- [ ] Anomaly list as its own view (rows currently live in Warnings).
- [ ] Rename visualizer → analyzer (deferred by followup2 #5).

## Verification
- [x] `cargo test` in `core/` — 28 tests (new: overlap implicit end, span
  matching, logs, v1-key skipping, run merge, demo round-trip)
- [x] `./build.sh` — wasm builds and staged into `web/`
- [x] bridge exercised end-to-end with curl (auth, list, read, write,
  traversal, preflight)
- [ ] Manual smoke-test in browser (Bar)

# UX batch 2 (2026-07-14)

- [x] **Logs view** — terminal-style scrollback (monospace, level-colored
  badges, src hints); lines after the playhead dim ("not printed yet"), the
  newest printed line is marked and followed (toggle); text filter; click a
  line to seek. "Logs" toolbar button, docks bottom by default.
- [x] **Active spans view** — the span stack open at the playhead, grouped
  by lane with nesting indentation and open-duration; click selects the
  span. "Active" toolbar button, docks right by default.
- [x] **Worker/wasm cache-busting** — worker.js and the .wasm load with a
  per-load ?v= param, so a rebuild shows up on plain F5 (no more devtools
  disable-cache dance).
- [x] **Docked-by-default windows** — every window opens docked at a natural
  side (events/logs/warnings → bottom, allocation/span/marks/active → right,
  play/layout/filter/lanes/project → left); dragging one out opts it out
  (remembered per session).
- [x] **Bottom dock** — third drawer under the views; panels stack
  side-by-side with draggable splits; drawer height resizable; drag-and-drop
  docking with insertion preview works into all three docks.
- [x] **Dock hide/show** — ◧ ⬓ ◨ toggles in the status bar collapse a dock
  while keeping its windows locked inside (editor-style); opening or
  docking a window into a collapsed dock reveals it; state persists.
- [x] **Fixes** — bottom drawer spanned only 300px (inherited .drawer width;
  now full-width); lane strip scrolls vertically when the lane stack is
  taller than its cap (canvas laid out at content height, hit-tests in
  content coords); span/log window got the allocation panel's pinning
  lifecycle (📌 keeps the window, dedupe per span/log, unpin returns it to
  the live panel, cleared on trace load).
