1. I want to support browsers without file access api - by giving users a bridge that they can run locally and the visualizer will work without needing to access the file system. It will support both file access api and access through the bridge.

Open questions:
1. lets defer it for later version.
2. yes, lets add them.
3. explicit manifest sounds good.
4. lets offer save as project, ut allow ephemeral mode.
5. I will want to rename it, but lets do it after landing the changes.
