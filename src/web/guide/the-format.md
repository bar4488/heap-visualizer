# Produce a trace

- [Download the annotated sample](guide/traces/format.heapl).
- Emit one JSON object per line.
- Start with a header, then write malloc, free, realloc, and optional custom events.
- Open the result here and check Warnings for malformed records.
